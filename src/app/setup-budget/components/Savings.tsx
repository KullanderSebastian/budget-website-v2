import React from "react";

export default function Savings({ onPrev, onNext }: { onPrev: () => void, onNext: () => void }) {
    return (
        <div>
            <h1>Savings</h1>
        </div>
    )
}