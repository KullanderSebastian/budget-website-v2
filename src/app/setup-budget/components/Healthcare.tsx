import React from "react";

export default function Healthcare({ onPrev, onNext }: { onPrev: () => void, onNext: () => void }) {
    return (
        <div>
            <h1>Healthcare</h1>
        </div>
    )
}