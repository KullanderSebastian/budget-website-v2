import React from "react";

export default function DebtLoans({ onPrev, onNext }: { onPrev: () => void, onNext: () => void }) {
    return (
        <div>
            <h1>DebtLoans</h1>
        </div>
    )
}