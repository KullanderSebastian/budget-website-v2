import React from "react";

export default function Emergencies({ onPrev, onNext }: { onPrev: () => void, onNext: () => void }) {
    return (
        <div>
            <h1>Emergencies</h1>
        </div>
    )
}