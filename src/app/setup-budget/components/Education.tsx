import React from "react";

export default function Education({ onPrev, onNext }: { onPrev: () => void, onNext: () => void }) {
    return (
        <div>
            <h1>Education</h1>
        </div>
    )
}